# Github Profile Analyser

Simply get a profile input by the user, parse it and return the preferred language of the github user.

# Notes

Based on angular, just get the html and the js and it will do the job. Nothing heavy, nothing to install or configure.